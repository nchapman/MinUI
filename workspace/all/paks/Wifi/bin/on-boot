#!/bin/sh
# Boot-time WiFi startup hook

BIN_DIR="$(dirname "$0")"
PAK_DIR="$(dirname "$BIN_DIR")"
PAK_NAME="$(basename "$PAK_DIR" .pak)"

rm -f "$LOGS_PATH/$PAK_NAME.on-boot.txt"
exec >>"$LOGS_PATH/$PAK_NAME.on-boot.txt" 2>&1

echo "$0" "$@"
cd "$PAK_DIR" || exit 1

"$BIN_DIR/service-on" &
